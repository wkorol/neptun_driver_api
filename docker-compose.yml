services:
  base:
    image: neptun-base:latest
    build:
      context: ./
      dockerfile: ./docker/base/Dockerfile
  application:
    depends_on:
      - base
    build:
      context: ./
      dockerfile: ./docker/dev/Dockerfile
    image: neptun-backend:latest
    volumes:
      - ./docker/dev/vhost.conf:/etc/apache2/sites-enabled/000-default.conf
      - .:/home/www
    networks:
      - default
    ports:
      - "8000:80"
  database:
    image: postgres:16
    environment:
      POSTGRES_PASSWORD: main
      POSTGRES_USER: main
      POSTGRES_DB: main
    ports:
      - "5432:5432"
    volumes:
      - neptun-postgresql:/var/lib/postgresql/data
volumes:
  neptun-postgresql:
    external: true
